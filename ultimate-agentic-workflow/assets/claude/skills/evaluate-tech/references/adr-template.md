# DEC-NNN: <adopt | reject | hold> <technology> for <surface>

**Date:** YYYY-MM-DD · **Status:** decided · **Radar state:** assess|trial|adopt|hold

## Context
What surfaced this (brief/HN/question), and what it would replace. The incumbent
and what is specifically wrong with it — or "nothing, tracking only".

## The claim
The headline claim, verbatim, with its source. Note whether the benchmark
methodology was read and against what baseline.

## What I measured
The spike: what was wired behind what adapter, in which worktree. The eval:
which golden set, the side-by-side numbers (candidate vs incumbent on your data).
If measurement was skipped, say why (and prefer `assess` over a guessed verdict).

## Decision
The verdict in one sentence.

## Revisit trigger
The specific condition that would reopen this: a date, a version ("when it ships
1.0"), a metric ("if our recall drops below X"), or an event ("if the maintainer
resumes"). This is the field that stops silent forgetting and endless re-litigation.

## Reversibility
How this backs out if wrong (adapter seam, feature flag, pinned version).
